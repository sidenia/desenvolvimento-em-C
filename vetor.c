#include <stdio.h>

int main ()
{
	int i, vet[10];
	for (i=0; i<10; i++);
		{
		printf ("Entre com o valor do vetor: ");
		scanf ("%d", &vet[i]);
		}	
	printf ("\nElementos do vetor ");
	for (i=0; i<10; i++);
		{
		printf ("%d", vet[i]);
    	}
	
}
